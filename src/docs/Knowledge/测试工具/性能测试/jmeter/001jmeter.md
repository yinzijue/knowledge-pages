---
title: 001jmeter
---
# 前言

## 关于组件执行顺序

**测试计划**>**线程组**>配置原件>前置处理器>定时器>**取样器**>后置处理器>断言>监听器

## 关于组件作用域

辅助组件可作用其父组件、同级组件及其同级组件下的所有子组件。

# 测试计划

## 使用场景

所有测试组件的起点，每个脚本中只能有一个测试计划。

## 参数配置

- 用户定义的变量：对整个测试计划有效，其优先级低于手动添加的【用户定义的变量】组件；
- 独立运行每个线程组：勾选后，保证按照顺序执行线程组；

# 线程组

# 配置原件

# 逻辑处理器

# 前置处理器

# 取样器

# 后置处理器

# 断言

# 定时器

# 监听器

# CLI命令

# 配置文件