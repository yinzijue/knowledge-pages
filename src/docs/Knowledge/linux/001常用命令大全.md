---
title: 001常用命令大全
---
# 前言

- linux命令**严格区分大小写**。通常未作特殊声明，命令一律使用小写字母。
- linux命令通常的格式为 **命令 [选项] [参数]**

# 组装命令

## xargs

### 概述

xargs的英文全拼为**extended arguments**,是给命令传递参数的一个过滤器，也是组合多个命令的一个工具。xargs可以将管道或标准输入(stdin)数据转换为命令行参数，也能够从文件的输出中读取数据。对于它组合命令的功能，说人话就是类似于测试多个接口时，将上一个接口的部分响应内容作为下一个接口的入参。

### 命令参数

- -a file 从文件中读入作为 stdin
- -e flag ，注意有的时候可能会是-E，flag必须是一个以空格分隔的标志，当xargs分析到含有flag这个标志的时候就停止。
- -p 当每次执行一个argument的时候询问一次用户。
- -n num 后面加次数，表示命令在执行的时候一次用的argument的个数，默认是用所有的。
- -t 表示先打印命令，然后再执行。
- -i 或者是-I，这得看linux支持了，将xargs的每项名称，一般是一行一行赋值给 {}，可以用 {} 代替。
- -r no-run-if-empty 当xargs的输入为空的时候则停止xargs，不用再去执行了。
- -s num 命令行的最大字符数，指的是 xargs 后面那个命令的最大命令行字符数。
- -L num 从标准输入一次读取 num 行送给 command 命令。
- -l 同 -L。
- -d delim 分隔符，默认的xargs分隔符是回车，argument的分隔符是空格，这里修改的是xargs的分隔符。
- -x exit的意思，主要是配合-s使用

### 命令对象

stdin,管道，文件输出等。

### 实例

```shell
rpm -qa|grep python|xargs rpm -ev --allmatches --nodeps #强制删除已安装的python及其关联
whereis python|xargs rm -frv #将whereis命令的结果作为rm命令的命令对象
```

# 登录与登出

## poweroff

# 文件管理

## ls

### 概述

ls命令的英文全拼为**list**，用于显示指定工作目录下的内容(目录、子目录、文件)。

### 命令参数

- -a(-all):显示所有文件及目录(.开头的影藏文件也会列出)；
- -A:同-a,但不列出当前目录和上级目录；
- -l:显示为long format(长格式)，将文件形态、权限、拥有者、文件大小等信息列出；
- -r(-reverse):将文件以相反次序显示(原定依英文字母次序)；
- -R(-recursion)：递归列出目标目录下的所有文件；
- -t(-time):将文件依建立时间的先后顺序列出；
- -F:在列出的文件名称后加一个符号，例如目录加“/”，可执行文件则加“*”

### 命令对象

目录、文件

### 实例

```shell
ls /  #列出根目录下的所有目录
ls -ltr s* #列出目前工作目录下所有名称是 s 开头的文件，越新的排越后面
ls -lR /bin #将 /bin 目录以下所有目录及文件详细资料列出
ls -AF #列出目前工作目录下所有文件及目录；目录于名称后加 "/", 可执行档于名称后加 "*"
```

## cd

### 概述

cd命令的英文全拼为**change directory，**用于切换当前工作目录。

### 命令参数

无

### 命令对象

目录。以下是一些常用目录的表达方式：

- /:根目录；
- .:当前目录；
- ..:上级目录；
- ~:当前用户的家目录；

## pwd

### 概述

pwd命令的英文全拼为**print work directory,**用于显示工作目录

### 命令参数

- -L:显示逻辑路径(忽略软链接文件)，取pwd系统环境变量中的值；
- -P:显示物理路径，若当前目录路径是软链接文件，则显示软链接对应的源文件；

以上两个参数不常用，只作了解，通常pwd不带参数。

### 命令对象

无

## mkdir

### 概述

mkdir命令的英文全拼为**make directory，**用于创建目录；

### 命令参数

- -p:用于确保要创建的目录所在的路径中所有的目录都存在，若不存在则自动创建那些缺失的目录。例如：mkdir /a/b/c 若a或b其中之一不存在，且未使用-p参数，则产生错误。

### 命令对象

待创建的目录；

## touch

### 概述

touch用于修改文件或目录的时间属性，包括存取和修改时间。若文件不存在，则创建一个新的该文件；

### 命令参数

- -a(access time):改变文件的读取时间记录；
- -m(modification time):改变文件的修改时间记录；
- -c:假如目标文件不存在，则不创建新的文件，效果同--no-create
- -r:使用参考文件的时间来设定目标文件的时间记录，使两者具有相同的时间记录。参考文件写前面；

### 命令对象

文件、目录

## mv

### 概述

mv命令的英文全拼为**move file**，用来为文件或目录重命名、或移动文件/目录到其他位置。其中**源路径在前，目标路径在后**。

### 命令参数

- -b:当目标文件或目录已存在时，执行覆盖前会先创建一个备份；
- -i：执行覆盖前选询问；
- -f：不询问直接覆盖；
- -n：不覆盖任何已存在的文件或目录；
- -u：当源文件比目标文件新或这目标文件不存在时，才执行移动操作；

### 命令对象

文件、目录

### 实例

```shell
mv source_file dest_file                 #将源文件名 source_file 改为目标文件名 dest_file
mv source_file dest_directory      #将文件 source_file 移动到目标目录 dest_directory 中
mv source_directory(目录) dest_directory(目录)      #目录名 dest_directory 已存在，将 source_directory 移动到目录名 dest_directory 中；目录名 dest_directory 不存在则 source_directory 改名为目录名 dest_directory
mv source_directory(目录) dest_file(文件)      #出错
```

## rm

### 概述

rm命令的英文全拼为**remove**，用于删除一个文件或目录；

### 命令参数

- -i：删除前逐一确认；
- -f：强制删除，即使目标为只读；
- -r：递归删除目录下所有内容；

### 命令对象

文件、目录

### 实例

```shell
rm filename # 是否删除文件
rm -r dirname #递归删除目录下的内容，删除目录必须使用-r参数
```

## cp

### 概述

cp命令的英文全拼为**copy file**，主要用于复制文件或目录。其中源在前，目标在后。

### 命令参数

- -a(all):该参数常用于复制目录，保留链接、文件属性，并复制目录下的所有内容；
- -d:复制时保留链接；
- -f:覆盖已存在的目标文件或目录而不给出提示；
- -p:复制文件时将修改时间和访问权限复制到新文件；
- -r:若源文件是目录，则递归复制目录下所有内容；
- -l:不复制文件，只是**生成链接文件**。

### 命令对象

文件、目录

### 实例

```shell
cp -r source dest #将source复制到dest,若source为目录，则必须使用-r参数。
```

## ln

### 概述

ln命令的英文全拼是**link files**，用于为文件建立链接，该链接的功能相当于windows中的快捷方式。

#### 软链接

#### 硬链接

### 命令参数

## chattr

### 概述

chattr命令的英文全拼为**change attribute**,用于改变文件的属性，这些属性共有以下8种模式：

- a:让文件或目录仅供附加用途；
- b:不更新文件或目录的最后存取时间；
- c:将文件或目录压缩后存放；
- d:将文件或目录排除在倾倒操作之外；
- i：不得任意更改文件或目录；
- s:保密性删除文件或目录；
- S:即时更新文件或目录；
- u:预防意外删除；

### 命令参数

- -R:递归处理；
- -v:后面跟版本编号，设置文件或目录的版本；
- -V:显示指令执行过程；
- +<属性>:开启文件或目录的某项属性；
- -<属性>:关闭文件或目录的某项属性；
- =<属性>:指定文件或目录的某项属性；

### 命令对象

文件、目录

### 实例

```shell
chattr +i filename or dirname #防止文件或目录被篡改；
chattr +a filename or dirname #设置某文件或目录只能追加数据，不能删除，适用于各种日志；
```

## lsattr

### 概述

lsattr命令的英文全拼是**list attribute**，用于列出文件或目录的属性。

### 命令对象

文件、目录

### 实例

```shell
lsattr filename or dirname #列出文件或目录的属性；
```

## file

### 概述

file命令用于显示文件的类型。

### 命令参数

- -b 列出辨识结果时，不显示文件名称。
- -c 详细显示指令执行过程，便于排错或分析程序执行的情形。
- -f<名称文件> 指定名称文件，其内容有一个或多个文件名称时，让file依序辨识这些文件，格式为每列一个文件名称。
- -L 直接显示符号连接所指向的文件的类别。
- -m<魔法数字文件> 指定魔法数字文件。
- -v 显示版本信息。
- -z 尝试去解读压缩文件的内容。
- [文件或目录...] 要确定类型的文件列表，多个文件之间使用空格分开，可以使用shell通配符匹配多个文件。

### 命令对象

文件、目录

# 文档编辑

## cat

### 概述

cat命令的英文全拼为**concatenate**，用于连接文件并打印在标准输出设备上；

### 命令参数

- -n 或 --number：由 1 开始对所有输出的行数编号。
- -b 或 --number-nonblank：和 -n 相似，只不过对于空白行不编号。
- -s 或 --squeeze-blank：当遇到有连续两行以上的空白行，就代换为一行的空白行。
- -v 或 --show-nonprinting：使用 ^ 和 M- 符号，除了 LFD 和 TAB 之外。
- -E 或 --show-ends : 在每行结束处显示 $。
- -T 或 --show-tabs: 将 TAB 字符显示为 ^I。
- -A, --show-all：等价于 -vET。
- -e：等价于"-vE"选项；
- -t：等价于"-vT"选项；

### 命令对象

文件

### 实例

```shell
cat -n textfile1 > textfile2 #把 textfile1 的文档内容加上行号后输入 textfile2 这个文档里
cat -b textfile1 textfile2 >> textfile3 #把 textfile1 和 textfile2 的文档内容加上行号（空白行不加）之后将内容附加到 textfile3 文档里
cat /dev/null > /etc/test.txt #清空 /etc/test.txt 文档内容
```

## tail

### 概述

tail命令用于查看文件内容。

### 命令参数

- -f 循环读取
- -q 不显示处理信息
- -v 显示详细的处理信息
- -c<数目> 显示的字节数
- -n<行数> 显示文件的尾部 n 行内容
- --pid=PID 与-f合用,表示在进程ID,PID死掉之后结束
- -q, --quiet, --silent 从不输出给出文件名的首部
- -s, --sleep-interval=S 与-f合用,表示在每次反复的间隔休眠S秒

### 命令对象

文件

### 实例

```shell
tail notes.log #显示notes日志文件的最后10行
tail -f notes.log #持续显示notes日志文件的最新后10行
tail -n +20 notes.log #显示从20行至文件末尾
tail -c 20 notes.log #显示notes日志文件的最后20行
```

## grep

### 概述

grep命令的全拼是**Global search Regular Expression and Print out the line**。用于全面的在范围内查找符合指定范式的文件。其功能侧重于单纯地查找。

### 命令参数

grep命令参数众多，这里只列举常用的几个：

- -i(**ignoreCase**):忽略字符大小写；
- -o:仅显示匹配到的字符串本身；
- -v:显示不能被范式匹配到的行，效果等同于--invert-match；
- -E:支持使用正则表达式作为查找范式，效果等同于--regexp=<范本样式>；
- -q:静默模式，不输出任何信息；

### 命令对象

文件

### 实例

```shell
grep test *file #在当前目录中，查找后缀有 file 字样的文件中包含 test 字符串的文件，并打印出该字符串的行
grep -r update /etc/acpi  #递归查找
grep -v test *test* #查找文件名中包含 test 的文件中不包含test 的行
```

## awk

### 概述

awk是一种处理文本文件的语言，是一个强大的文本分析工具，awk是取自三位创始人的名字首字母。其功能更适合格式化文本，对文本进行较复杂的格式处理

### 命令参数

## sed

### 概述

sed命令通常会利用脚本来处理文本文件，主要用于自动编辑一个或多个文件，简化对文件的重复操作、编写转换程序等。常见的动作说明：

- a ：新增， a 的后面可以接字串，而这些字串会在新的一行出现(目前的下一行)～
- c ：取代， c 的后面可以接字串，这些字串可以取代 n1,n2 之间的行！
- d ：删除，后面通常不接任何东西；
- i ：插入， i 的后面可以接字串，而这些字串会在新的一行出现(目前的上一行)；
- p ：打印，亦即将某个选择的数据印出。通常 p 会与参数 sed -n 一起运行～
- s ：取代，可以直接进行取代的工作，通常这个 s 的动作可以搭配正则表达式；

### 命令参数

- -e<script>或--expression=<script> 以选项中指定的script来处理输入的文本文件。
- -f<script文件>或--file=<script文件> 以选项中指定的script文件来处理输入的文本文件。
- -h或--help 显示帮助。
- -n或--quiet或--silent 仅显示script处理后的结果。
- -V或--version 显示版本信息。

### 命令对象

文件

### 实例

## cut

### 概述

cut命令用于显示每行从开头算起num1到num2的文字。

### 命令参数

- -b ：以字节为单位进行分割。这些字节位置将忽略多字节字符边界，除非也指定了 -n 标志。
- -c ：以字符为单位进行分割。
- -d ：自定义分隔符，默认为制表符。
- -f ：与-d一起使用，指定显示哪个区域。
- -n ：取消分割多字节字符。仅和 -b 标志一起使用。如果字符的最后一个字节落在由 -b 标志的 List 参数指示的  
    范围之内，该字符将被写出；否则，该字符将被排除

### 命令对象

字符串

### 实例

```shell
>$ who
>root
>$ who|cut -b 3
>o
```

## diff

### 概述

diff命令的英文全拼为**difference**，用于比较文件的差异。它以逐行的方式比较文件差异，如果指定的是目录，则其会比较目录中文件名相同的文件，但不会比较其中的子文件。

### 命令参数

- -i:忽略大小写，效果等同于--ignore-case；
- <行数>:指定要显示多少行的文本。此参数必须与-C或-u参数一并使用；
- -b:不检查空格字符的不同，效果等同于--ignore-space-change；
- -B:不检查空白行，效果等同于--ignore-blank-lines.
- -c:显示全部内容；
- -C<行数>：显示指定行数，与执行-c-<行数>指令相同，效果等同于--context<行数>;

### 命令对象

文件、目录

### 实例

```shell
diff a.log b.log -y -W 50 #以50宽度，并列的方式显示比较结果；
```

## tr

### 概述

tr命令的英文全拼为**Text Replacer**，该命令用于文本替换。

### 命令参数

- -c, --complement：反选设定字符。也就是符合 SET1 的部份不做处理，不符合的剩余部份才进行转换
- -d, --delete：删除指令字符
- -s, --squeeze-repeats：缩减连续重复的字符成指定的单个字符
- -t, --truncate-set1：削减 SET1 指定范围，使之与 SET2 设定长度相等
- --help：显示程序用法信息
- --version：显示程序本身的版本信息

### 命令对象

文本内容

### 实例

```shell
cat testfile |tr a-z A-Z #将testfile中所有的小写字母全部转换为大写字母
cat testfile |tr [:upper:] [:lower:] #将testfile中所有的大写字母转换为小写字母
```

## sort

### 概述

sort命令可以对文本内容进行排序

### 命令参数

- -f：忽略大小写；
- -b：忽略每行前面的空白部分；
- -n：以数值型进行排序，默认使用字符串排序；
- -r： 反向排序, 默认是正序；
- -u：删除重复行。就是 uniq 命令；
- -t：指定分隔符，默认分隔符是制表符；
- -k [n,m]：按照指定的字段范围排序。从第 n 个字段开始，到第 m 个字(默认到行尾)

### 命令对象

文本内容

### 实例

```shell
sort testfile -k 2 #对文件的第二列的值重新排序
```

## uniq

### 概述

uniq命令的英文全拼为unique，用于检查及删除文本文件中重复出现的行列，一般与sort命令结合使用。

### 命令参数

- -c或--count 在每列旁边显示该行重复出现的次数。
- -d或--repeated 仅显示重复出现的行列。
- -f<栏位>或--skip-fields=<栏位> 忽略比较指定的栏位。
- -s<字符位置>或--skip-chars=<字符位置> 忽略比较指定的字符。
- -u或--unique 仅显示出一次的行列。
- -w<字符位置>或--check-chars=<字符位置> 指定要比较的字符。

### 命令对象

文本内容

### 实例

```shell
uniq -c testfile #检查并删除testfile文件中重复的行，结果显示为剩余内容，及剩余内容前面重复的次数
```

## paste

### 概述

paste命令用于合并文件的列。

### 命令参数

- -d<间隔字符>或--delimiters=<间隔字符> 用指定的间隔字符取代跳格字符。
- -s或--serial 串列进行而非平行处理。

### 命令对象

文件

### 实例

```shell
paste -s file #将file文件中的所有内容合并为一行显示(不改变源文件内容)
```

# 文件传输

## scp

### 概述

scp命令的英文全拼为**secure copy,**用于在linux之间安全的复制文件和目录。另外，rcp命令是不加密的。

### 命令参数

- 1： 强制scp命令使用协议ssh1
- -2： 强制scp命令使用协议ssh2
- -4： 强制scp命令只使用IPv4寻址
- -6： 强制scp命令只使用IPv6寻址
- -B： 使用批处理模式（传输过程中不询问传输口令或短语）
- -p：保留原文件的修改时间，访问时间和访问权限。
- -r： 递归复制整个目录。
- -v：详细方式显示输出。scp和ssh(1)会显示出整个过程的调试信息。这些信息用于调试连接，验证和配置问题。
- -i identity_file： 从指定文件中读取传输时使用的密钥文件，此参数直接传递给ssh。
- -P port：注意是大写的P, port是指定数据传输用到的端口号

### 命令对象

文件、目录

### 实例

```shell
scp -P PORT local_file remote_username@remote_ip:remote_folder #指定端口从本地发送文件到远程
```

## ftp

### 概述

ftp命令用于设置linux文件系统的相关功能。

### 命令参数

- -d 详细显示指令执行过程，便于排错或分析程序执行的情形。
- -i 关闭互动模式，不询问任何问题。
- -g 关闭本地主机文件名称支持特殊字符的扩充特性。
- -n 不使用自动登陆。
- -v 显示指令执行过程。

### 命令对象

ftp地址

### 实例

```shell
ftp ftp.kernel.org #发起链接请求
```

# 磁盘管理

## df

### 概述

df命令的英文全拼为**disk free**,该命令用于显示目前linux系统上的文件系统磁盘使用情况统计。df命令默认显示的内容有：**文件系统名称、以KB为单位的总内存统计、已使用内存统计、可用内存统计、内存使用率统计、挂载点名称**。

df一般不跟命令对象，但也可以跟文件，用于显示文件所在文件系统的使用情况。

### 命令参数

- -a：全部文件系统列表，包含虚拟文件系统
- -B：指定显示scale size，比如以M，G为单位显示
- -h：方便人阅读方式显示。
- -H：和-h相似，不过其中1k = 1000byte， 1M=1000k
- -i：列出inode的信息
- -k ：区块为1024字节
- -l ：显示本地文件系统
- -p：使用POSIX规范输出
- -t：打印指定的文件系统类型
- -T：显示文件系统类型
- -x：不打印指定的文件系统类型

### 命令对象

通常不跟对象，或跟文件。

### 概述

```shell
df -BM #以MB为单位，统计所有文件系统的使用情况。
```

## du

### 概述

du命令的英文全拼为**disk used**,用于显示目录或文件的大小。

### 命令参数

- -h：以人类可读的方式显示
- -a：显示目录占用的磁盘空间大小，还要显示其下目录和文件占用磁盘空间的大小
- -s：显示目录占用的磁盘空间大小，不要显示其下子目录和文件占用的磁盘空间大小
- -c：显示几个目录或文件占用的磁盘空间大小，还要统计它们的总和
- --apparent-size：显示目录或文件自身的大小
- -l ：统计硬链接占用磁盘空间的大小
- -L：统计符号链接所指向的文件占用的磁盘空间大小

### 命令对象

文件、目录

### 实例

```shell
du -sh testfile #以合适的形式显示testfile文件的总计大小
```

## fdisk

### 概述

fdisk是用于创建和维护分区表的一款工具，其功能类似于windows中的**磁盘管理**。fdisk后接具体的设备，可以进入fdisk菜单，常用的菜单指令有：

- m ：显示菜单和帮助信息
- a ：活动分区标记/引导分区
- d ：删除分区
- l ：显示分区类型
- n ：新建分区
- p ：显示分区信息
- q ：退出不保存
- t ：设置分区号
- v ：进行分区检查
- w ：保存修改
- x ：扩展应用，高级功能

### 命令参数

- -l 列出素所有分区表
- -u 与 **-l** 搭配使用，显示分区数目
- -s<分区编号> 指定分区(非必选参数)
- -v 版本信息(非必选参数)

### 命令对象

设备

### 实例

```shell
fdisk -l #显示当前分区情况
fdisk -lu #显示SCSI硬盘的每个分区情况。
fdisk /dev/sda #后跟设备sda，进入fdisk菜单
```

## mount

### 概述

mount命令用于挂载linux系统外的文件。例如，插入的U盘，通常会被识别为SCSI(small computer system interface)磁盘。将该磁盘挂载在某一路径下，便可以通过该路径访问磁盘中的所有文件。**类似于windows系统插入U盘后，系统自动分配的盘符**。

### 命令参数

- -o <option>:指定挂载文件系统时的选项，常见的选项有：

- defaults ：使用所有选项的默认值(auto,nouser,rw,suid)
- ro:以只读方式挂载；
- rw:以读写方式挂载；
- remount:重新安装已经安装了的文件系统；

### 命令对象

存储设备

### 实例

```shell
mount -o ro /dev/hda1 /mnt #分区/dev/hda1以只读方式挂载在/mnt下
```

# 网络通讯

## wget

### 概述

wget命令是linux系统从web下载文件的命令行工具，支持http、https、ftp等协议。其名字来源于www(world wide web)

### 命令参数

- -c：支持断点续传;
- -b:后台下载文件;
- --spider:测试下载链接；
- -o:将下载信息存入日志；
- -O:对下载文件进行重命名;
- -P:将文件下载到指定目录路径下；
- -i <file>:file文件中每行存储一个下载链接，支持批量下载；
- (更多参数使用--help查看)

### 命令对象

URL

### 实例

```shell
wget --spider url #测试URL指向的资源是否可用
wget -i download_list.txt #批量下载文件中链接指向的资源
```

## telnet

### 概述

telnet命令用于远端登入。

### 命令参数

- -l:指定远程登录的用户名；
- -b:使用别名指定远程主机的名称；
- -n:指定一个跟踪文件用于记录跟踪信息；

### 命令对象

远程连接

### 实例

```shell
telnet ip #登录ip指向的远程主机
```

## ss

### 概述

### 命令参数

### 命令对象

### 实例

## netstat

### 概述

netstat命令的英文全拼为**net status**,用于显示网络状态。

### 命令参数

- -a:显示所有连线中的socket；
- -c:持续列出网络状态，效果等同于--continuous；
- -l:显示监控中的服务器的socket，效果等同于--listening；
- -i:显示网络界面信息表单
- -n:直接使用ip地址，而不是域名服务器，效果等同于--netlink或者--symbolic；
- -o:显示计时器，效果等同于--timers；
- -p:显示正在使用socket的程序识别码和程序名称；
- -r:显示路由表，效果等同于--route；
- -t:显示tcp传输协议的连线情况；

### 命令对象

通常为空。

### 实例

```shell
netstat -anpt #以数字形式显示当前系统中所有的tcp连接信息
netstat -i #显示网卡列表
netstat -s #显示网络统计信息
```

## ping

### 概述

ping命令用于检测主机的连通性。

### 命令参数

- -d:使用socket的SO_DEBUG功能；
- -c <times>:设置完成要求回应的次数；
- -f:极限检测;

### 命令对象

主机地址

### 实例

```shell
ping -i 3 -s 1024 -t 255 ip #发送周期3秒，发送包的大小为1024，TTL值为255
```

## httpd

### 概述

httpd命令是apache http服务器程序。

### 命令参数

- -t:检查配置文件中的语法错误;
- -L 显示httpd指令的说明。
- -d<服务器根目录> 指定服务器的根目录。
- -D<设定文件参数> 指定要传入配置文件的参数。
- -f<设定文件> 指定配置文件。

### 命令对象

通常为空，或跟配置文件。

### 实例

```shell
httpd -t #检查配置文件中的语法错误
httpd -L>1.log|tail -n 20 1.log #显示配置文件
```

## wall

### 概述

wall命令会将message传给每一个mesg设定为yes的上线使用者。

### 命令参数

无

### 命令对象

message

### 实例

```shell
wall hi #将信息hi广播
```

## ifconfig

### 概述

ifconfig命令的英文全拼为**network interfaces configuring**，用于显示或配置网络设备。

### 命令参数

- add<地址>：设置网络设备IPv6的ip地址。
- del<地址>：删除网络设备IPv6的ip地址。
- down：关闭指定的网络设备。
- up：启动指定的网络设备。
- netmask<子网掩码>：设置网络设备的子网掩码。
- io_addr：设置网络设备的I/O地址。
- irq：设置网络设备的IRQ。
- mtu<字节>：设置网络设备的MTU。
- hw:设置网络设备的类型与硬件地址

### 命令对象

网络设备

### 实例

```shell
ifconfig eth0 up #开启网卡
ifconfig eth0 127.0.0.1 netmask 255.255.255.0 #给网卡配置ip地址并设置子网掩码
ifconfig eth0 arp #启用对应网卡的ARP协议
ifconfig eth0 -arp #关闭对应网卡的ARP协议
ifconfig eth0 hw ether 00:AA:BB:CC:DD:EE #为硬件类型为ether的设备设置MAC地址
```

# 系统管理

## w

### 概述

w(**小写**)命令用于显示目前登入系统的用户信息以及这些用户正在执行的程序。此外，w命令还可以显示系统运行总时长、用户在线数、系统平均负载。从输出结果来看，w命令的输出结果完全包含了uptime命令的输出结果。

### 命令参数

- -f 开启或关闭显示用户从何处登入系统。
- -h 不显示各栏位的标题信息列。
- -l 使用详细格式列表，此为预设值。
- -s 使用简洁格式列表，不显示用户登入时间，终端机阶段作业和程序所耗费的CPU时间。
- -u 忽略执行程序的名称，以及该程序耗费CPU时间的信息。
- -V 显示版本信息。

### 命令对象

通常为空，或者指定用户。

## free

### 概述

free命令用于显示内存状态，包括实体内存、虚拟的交换文件内存、共享内存区段，以及系统核心使用的缓冲区等。

### 命令参数

- b 以Byte为单位显示内存使用情况。
- -k 以KB为单位显示内存使用情况。
- -m 以MB为单位显示内存使用情况。
- -h 以合适的单位显示内存使用情况，最大为三位数，自动计算对应的单位值。单位有B、K、M、G、T
- -o 不显示缓冲区调节列。
- -s<间隔秒数> 持续观察内存使用状况。
- -t 显示内存总和列。
- -V 显示版本信息。

### 命令对象

通常为空

### 实例

```shell
free -s 10 #每10S执行一次命令
```

## top

### 概述

top命令能够实时显示系统各个进程的资源占用情况。

### 命令参数

- d : 改变显示的更新速度，或是在交谈式指令列( interactive command)按 s
- q : 没有任何延迟的显示速度，如果使用者是有 superuser 的权限，则 top 将会以最高的优先序执行
- c : 切换显示模式，共有两种模式，一是只显示执行档的名称，另一种是显示完整的路径与名称
- S : 累积模式，会将己完成或消失的子进程 ( dead child process ) 的 CPU time 累积起来
- s : 安全模式，将交谈式指令取消, 避免潜在的危机
- i : 不显示任何闲置 (idle) 或无用 (zombie) 的进程
- n : 更新的次数，完成后将会退出 top
- b : 批次档模式，搭配 "n" 参数一起使用，可以用来将 top 的结果输出到档案内

### 命令对象

无

## mpstat

### 概述

mpstat命令的英文全称为**Multiprocessor statistics**，用来查询每个CPU的性能指标以及所有CPU的平均指标。

### 命令参数

- -P:指定要监控的CPU，范围是[0,n-1]

### 命令对象

cpu

### 实例

```shell
mpstat -P ALL 3 2 #监控所有CPU，每隔三秒采集一次信息，共采集2次
```

# 用户与权限

## 用户管理

### useradd

#### 概述

useradd用于建立用户账号，之后再用password命令设定密码，即可使用账号。useradd创建的账号保存在/etc/passwd文本文件中。

#### 命令参数

- -c<备注> 加上备注文字。备注文字会保存在passwd的备注栏位中。
- -d<登入目录> 指定用户登入时的起始目录。
- -D 变更预设值．
- -e<有效期限> 指定帐号的有效期限。
- -f<缓冲天数> 指定在密码过期后多少天即关闭该帐号。
- -g<群组> 指定用户所属的群组。
- -G<群组> 指定用户所属的附加群组。
- -m 制定用户的登入目录。
- -M 不要自动建立用户的登入目录。
- -n 取消建立以用户名称为名的群组．
- -r 建立系统帐号。
- -s<shell> 指定用户登入后所使用的shell。
- -u<uid> 指定用户ID。

#### 命令对象

用户名

#### 实例

```shell
useradd tt #添加普通用户tt
useradd -g root tt #为提那家的用户指定其所属用户组
useradd -r tt #创建一个系统用户
useradd -d /home/myd tt #为创建的用户指定其home目录
useradd caojh -u 544 #为创建的用户制定ID
```

### userdel

#### 概述

userdel命令用于删除用户账号。

#### 用户参数

- -r:删除用户登入目录及目录中所有文件；

#### 命令对象

用户名

### usermod

#### 概述

usermod命令用于修改用户账号的各项设定。

#### 命令参数

- -c<备注> 修改用户帐号的备注文字。
- -d登入目录> 修改用户登入时的目录。
- -e<有效期限> 修改帐号的有效期限。
- -f<缓冲天数> 修改在密码过期后多少天即关闭该帐号。
- -g<群组> 修改用户所属的群组。
- -G<群组> 修改用户所属的附加群组。
- -l<帐号名称> 修改用户帐号名称。
- -L 锁定用户密码，使密码无效。
- -s<shell> 修改用户登入后所使用的shell。
- -u<uid> 修改用户ID。
- -U 解除密码锁定。

#### 命令对象

用户名

#### 实例

```shell
usermod -d /home/hnlinux root #更改登录目录
usermod -u 777 root #更改用户的uid
```

## 用户组管理

### groupadd

#### 概述

groupadd 命令用于创建一个新的工作组，新工作组的信息将被添加到系统文件中。相关文件：

- /etc/group 组账户信息。
- /etc/gshadow 安全组账户信息。
- /etc/login.defs Shadow密码套件配置。

#### 命令参数

- -g：指定新建工作组的 **id**；
- -r：创建系统工作组，系统工作组的组 ID 小于 500；
- -K：覆盖配置文件 **/etc/login.defs**；
- -o：允许添加组 ID 号不唯一的工作组。
- -f,--force: 如果指定的组已经存在，此选项将失明了仅以成功状态退出。当与 -g 一起使用，并且指定的 GID_MIN 已经存在时，选择另一个唯一的 GID（即 -g 关闭）。

#### 命令对象

用户组

#### 实例

```shell
groupadd －g 344 test #创建一个test用户组，并指定其组ID
```

### groupdel

#### 概述

Linux groupdel命令用于删除群组。

#### 命令参数

无

#### 命令对象

用户组

### groupmod

#### 概述

Linux groupmod命令用于更改群组识别码或名称。

#### 命令参数

- -g <群组识别码> 设置欲使用的群组识别码。
- -o 重复使用群组识别码。
- -n <新群组名称> 设置欲使用的群组名称。

#### 命令对象

用户组

#### 实例

```shell
groupadd linuxso #添加一个用户组
groupmod -n linux linuxso #修改用户组名
```

## 权限管理

### passwd

#### 概述

Linux passwd命令用来更改使用者的密码

#### 命令参数

- -d 删除密码
- -f 强迫用户下次登录时必须修改口令
- -w 口令要到期提前警告的天数
- -k 更新只能发送在过期之后
- -l 停止账号使用
- -S 显示密码信息
- -u 启用已被停止的账户
- -x 指定口令最长存活期
- -g 修改群组密码
- 指定口令最短存活期
- -i 口令过期后多少天停用账户

#### 命令对象

用户名

#### 实例

```shell
passwd test #设置用户密码
passwd -S test #显示用户信息
passwd -d test #删除用户密码
```

### chmod

#### 概述

chmod命令的英文全拼为**change mode，**用于修改用户对文件的权限。linux中的文件调用权限从前往后依次分为三级:所有者(Owner)、用户组(Group)、其他用户(Other Users)。权限类型也有三种：**r(read可读)=4、w(write可写)=2、x(execute可执行)=1**

#### 命令参数

- u 表示该文件的拥有者，g 表示与该文件的拥有者属于同一个群体(group)者，o 表示其他以外的人，a 表示这三者皆是。
- + 表示增加权限、- 表示取消权限、= 表示唯一设定权限。
- r 表示可读取，w 表示可写入，x 表示可执行，X 表示只有当该文件是个子目录或者该文件已经被设定过为可执行。
- -c : 若该文件权限确实已经更改，才显示其更改动作
- -f : 若该文件权限无法被更改也不要显示错误讯息
- -v : 显示权限变更的详细资料
- -R : 对目前目录下的所有文件与子目录进行相同的权限变更(即以递归的方式逐个变更)
- --help : 显示辅助说明
- --version : 显示版本

#### 命令对象

文件

#### 实例

```shell
chmod 4755 filename #使此文件具有root的权限
chmod -R a+r * #将当前目录及其子目录设置为任何人可读取
```

# 备份压缩

## tar

### 概述

tar命令的英文全拼是**tape archive**，用于建立备份文件。

### 命令参数

- -c:建立新的备份文件，等同于--create
- -x:从备份文件中还原文件，等同于--extract或者--get
- -z:通过gzip指令处理备份文件，等同于--gzip或者--ungizp
- -v:显示指令的执行过程，等同于--verbose
- -f:指定备份文件，等同于--file=<备份文件>
- -t:列出备份文件的内容，等同于--list
- -C:**指定解压后的文件的存放路径**

### 命令对象

文件

### 实例

```shell
tar -czvf test.tar.gz a.c  #压缩 a.c文件为test.tar.gz
tar -tzvf test.tar.gz #列出压缩文件中的内容
tar -xzvf test.tar.gz #解压文件
tar -xzvf test.tar.gz -C dest_path #将文件解压到dest_path路径下
```

## gzip

### 概述

gzip命令用于压缩文件。

### 命令参数

- -d或--decompress或----uncompress 解开压缩文件
- -l或--list 列出压缩文件的相关信息。
- -q或--quiet 不显示警告信息。
- -r或--recursive 递归处理，将指定目录下的所有文件及子目录一并处理。
- -t或--test 测试压缩文件是否正确无误。
- -v或--verbose 显示指令执行过程。
- -<压缩效率> 压缩效率是一个介于1－9的数值，预设值为"6"，指定愈大的数值，压缩效率就会愈高。
- --best 此参数的效果和指定"-9"参数相同。
- --fast 此参数的效果和指定"-1"参数相同。

### 命令对象

文件